const Notification = () => {
  // Define the styling for the notification box
  const style = {
    border: 'solid',
    padding: 10,
    borderWidth: 1
  }

  return (
    <div style={style}>
      render here notification... {/* Placeholder text for notifications */}
    </div>
  )
}

export default Notification
